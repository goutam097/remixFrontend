import { CalculateNumberInKPipe } from './calculate-number-in-k.pipe';

describe('CalculateNumberInKPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateNumberInKPipe();
    expect(pipe).toBeTruthy();
  });
});
